name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - run: npm run typecheck
      - run: npm test
      - name: Verify action bundle is current
        run: |
          if ! git diff --quiet actions/check/dist; then
            echo "::error::actions/check/dist is out of date — run 'npm run build --workspace=@envmanifest/action-check' and commit the result"
            git diff actions/check/dist
            exit 1
          fi
      - name: Dogfood — envmanifest doctor against this repo
        run: node packages/cli/bin/envmanifest.js doctor --env ci
