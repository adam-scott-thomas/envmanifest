{
  "name": "@envmanifest/action-check",
  "version": "0.1.3",
  "private": true,
  "description": "envmanifest GitHub Action — wraps `envmanifest check` with SARIF output and inline annotations.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@envmanifest/schema": "*",
    "ajv": "^8.12.0",
    "cac": "^6.7.14",
    "kleur": "^4.1.5",
    "yaml": "^2.4.1"
  }
}
