{
  "name": "@envmanifest/node",
  "version": "0.0.0",
  "description": "Typed runtime loader for envmanifest contracts.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "vitest": "^1.4.0"
  },
  "dependencies": {
    "@envmanifest/schema": "*"
  },
  "publishConfig": {
    "access": "public"
  }
}
