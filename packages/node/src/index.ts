export const VERSION = "0.0.0";

export function load(): never {
  throw new Error(
    "@envmanifest/node — not yet implemented (week 2). " +
      "Use `envmanifest generate types` once available.",
  );
}
