{
  "name": "@envmanifest/site",
  "version": "0.1.3",
  "private": true,
  "description": "Cloudflare Worker that serves env.ghostlogic.tech — schema files, .well-known endpoints, and a tiny landing page.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/embed-schema.mjs",
    "build": "tsup src/worker.ts --format esm --target esnext --clean --out-dir dist",
    "predev": "node scripts/embed-schema.mjs",
    "dev": "wrangler dev",
    "predeploy": "node scripts/embed-schema.mjs",
    "deploy": "wrangler deploy",
    "pretypecheck": "node scripts/embed-schema.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@envmanifest/schema": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240502.0",
    "wrangler": "^4.0.0"
  }
}
